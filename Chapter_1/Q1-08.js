let quotient, remainder
const devidend = parseInt("100.9")
let divisor = 3

quotient = Math./* ① */(devidend /* ② */ divisor)
remainder = devidend /* ③ */ divisor
console.log(quotient, remainder) // 出力結果： /* ④ */ 1

divisor = -3

quotient = Math./* ① */(devidend /* ② */ divisor)
remainder = devidend /* ③ */ divisor
console.log(quotient, remainder) // 出力結果： /* ⑤ */ 1
