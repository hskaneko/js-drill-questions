console.log("青空" ? "value" : "FALSY!") // 出力例：/* ① */
console.log("" ? "value" : "FALSY!") // 出力例：/* ② */
console.log(15 ? "value" : "FALSY!") // 出力例：/* ③ */
console.log(0 ? "value" : "FALSY!") // 出力例：/* ④ */
console.log(-1 ? "value" : "FALSY!") // 出力例：/* ⑤ */
console.log(0.1 ? "value" : "FALSY!") // 出力例：/* ⑥ */
console.log(0.0 ? "value" : "FALSY!") // 出力例：/* ⑦ */
console.log(null ? "value" : "FALSY!") // 出力例：/* ⑧ */
