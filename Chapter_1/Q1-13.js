let a = parseInt('string')
console.log(a == NaN) // 出力例：/* ① */
console.log(a === NaN) // 出力例：/* ② */
console.log(isNaN(a)) // 出力例：/* ③ */
console.log(typeof undefined)     // 出力例：/* ④ */
console.log(null === null)        // 出力例：/* ⑤ */
console.log(null === undefined)   // 出力例：/* ⑥ */
console.log(null !== undefined)   // 出力例：/* ⑦ */
console.log(null == undefined)   // 出力例：/* ⑧ */
